# This project doesn't require environment variables
# API keys are passed via request headers for security

# Optional: If you want to add rate limiting or other features
# RATE_LIMIT_PER_MINUTE=60

# Optional: For error tracking
# SENTRY_DSN=your_sentry_dsn_here

# Optional: For analytics
# NEXT_PUBLIC_GA_ID=your_google_analytics_id

# Webhook Configuration (optional)
# Default webhook URL for all buttons (Make.com webhook URL)
# NEXT_PUBLIC_DEFAULT_WEBHOOK_URL=https://hook.us1.make.com/your-webhook-id

# You can also configure button-specific webhooks in the code
# by editing the webhookUrls object in components/PropertyRenderer.tsx